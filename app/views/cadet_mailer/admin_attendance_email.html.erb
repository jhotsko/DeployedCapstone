<!DOCTYPE html>
<html lang="en">
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p><%= "Cadet #{@admin.lastName}" %>,</p>
    <p>
        Attendance for <%= @event.primaryType %> on <%= @event.eventDate %> has been updated.
        As a result, the following cadets' attendance records have been changed:
    </p>
    <% @event.attendances.each do |attendance| %>
      <% if attendance.attended == 0 || attendance.attended == 2 %>
        <strong><p> C/<%= attendance.cadet.firstName %> <%= attendance.cadet.lastName %>: </p></strong>
        <p>PT Attendance: <%= attendance.cadet.pt_attendance.round(2) %>% </p>
        <p>LLAB Attendance: <%= attendance.cadet.llab_attendance.round(2) %>% </p>
      <% end %>
    <% end %>
  </body>
</html>